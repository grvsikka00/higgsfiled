# Combined form: KEY_ID:KEY_SECRET
HF_CREDENTIALS=

# Or separate fields (used only if HF_CREDENTIALS is unset)
HF_API_KEY=
HF_API_SECRET=

# Optional override, defaults to https://platform.higgsfield.ai
HF_BASE_URL=
